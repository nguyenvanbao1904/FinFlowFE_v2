//
//  Identity.swift
//  Identity Module
//
//  Entry point và public exports cho Identity module
//

import Foundation

// MARK: - Public Exports
// Note: Các types đã được declare public trong các files riêng
// File này chỉ để tổ chức, không cần typealias

// Types are already public from their respective files:
// - LoginRequest, LoginResponse, UserProfile (Data layer)
// - AuthRepositoryProtocol, LoginUseCaseProtocol, etc. (Domain layer)
// - LoginViewModel, LoginView (Presentation layer)
// - AuthCoordinator, AuthRoute (Coordinator)
